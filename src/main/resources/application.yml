server:
  port: 9191

spring:
  datasource:
    url: jdbc:h2:mem:/order_db
    username: sa
    password:
    driverClassName: org.h2.Driver
  h2:
    console:
      enabled: true
      path: /h2-console
      settings:
        web-allow-others: true